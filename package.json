
{
    "scripts": {
        "frontend:install": "cd udagram-frontend && npm install --save --legacy-peer-deps",
        "frontend:build": "cd udagram-frontend && npm run build"
    },
    "devDependencies": {}
}